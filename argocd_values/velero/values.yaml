# Reference: https://github.com/vmware-tanzu/helm-charts/blob/main/charts/velero/values.yaml
# Version: 11.1.1

configuration:
  backupStorageLocation:
    - name: default
      provider: aws
      bucket: velero-backup-eks
      config:
        region: us-east-1
      # credential:
      #   name: test
      #   key: test

metrics:
  enabled: true
  scrapeInterval: 30s
  scrapeTimeout: 10s
